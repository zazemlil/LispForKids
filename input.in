(let
    (f (quote 5)) 
    (f 
        (
            lambda (a) (
                    cond 
                         (equal a (quote 0)) 
                         (quote 1) 
                         (mul (a) (f (sub a 1))) 
            )
        )
    )
)

# evalLetrec + equal для bool значений и quote() - NIL

