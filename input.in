(let 
     (drih m n) (
          drih (
               lambda (a b) (
                    cond 
                         (equal (rem a b) (quote 0)) 
                         (dive a b) 
                         (add (dive a b) 1)
               ) 
          )
     )
     (m (quote 3))
     (n (quote 2))
)

# evalLetrec

