(
    let (add (ff 2) 1)
        (f (
            lambda (n) (add (mul 2 2) 1 )
        ))
        (ff (
            lambda (a) (
                let (add a 2) (X (quote 1)) (b (quote 2))
            )
        ))
        (X (quote 1))
)

# evalLetrec

