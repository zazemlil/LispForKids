(letrec
     (pow (quote 2) (quote 8)) (
          pow (
               lambda (x y) (
                    cond
                         (equal y (quote 0))
                         (quote 1)
                         (mul 
                              x 
                              (pow x (sub y (quote 1)))
                         )
               )
          )
     )
)

# evalLetrec (не знаю, нужны ли ссылки в матрицах и в нодах при передаче)

